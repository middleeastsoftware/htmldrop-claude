---
description: List your published htmldrop sites
---

Call the `htmldrop_list` MCP tool to fetch every site the authenticated account owns.

Present the results as a compact table with columns: title, slug, public URL, and site id. If there are no sites, say so plainly.
